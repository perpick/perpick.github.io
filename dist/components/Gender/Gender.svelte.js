/* src/components/Gender/Gender.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "../../../_snowpack/pkg/svelte/internal.js";

function create_fragment(ctx) {
	let div;
	let p;
	let t_value = /*gender*/ ctx[0].type + "";
	let t;

	return {
		c() {
			div = element("div");
			p = element("p");
			t = text(t_value);
			attr(div, "class", "");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, p);
			append(p, t);
		},
		p(ctx, [dirty]) {
			if (dirty & /*gender*/ 1 && t_value !== (t_value = /*gender*/ ctx[0].type + "")) set_data(t, t_value);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { gender } = $$props;

	$$self.$$set = $$props => {
		if ("gender" in $$props) $$invalidate(0, gender = $$props.gender);
	};

	return [gender];
}

class Gender extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { gender: 0 });
	}
}

export default Gender;